export default interface CardInterface {
    id: string
    title: string
    description: string
    release_date: string
    poster: string
    category: string
    age_limit: string
    duration: string
    quality: string
}
